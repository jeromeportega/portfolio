import styled from 'styled-components';

export const IconLink = styled.a`
    text-decoration: none;
    color: inherit;

    &:hover, &:focus, &:active {
        text-decoration: none;
        color: inherit;
    }
`;